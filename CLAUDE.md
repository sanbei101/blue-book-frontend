# 开发规范

## 尽可能不写css,直接使用`shadcn`组件库
尽可能不要写复杂的`className`或者`css`样式,直接使用组件库的,保持代码干净,通过`mcp`进行查询想要的组件

## 禁止修改/新增`src/api`,必须通过`pnpm orval`进行生成,缺乏的接口需要和后端进行沟通

## 在使用api的时候全部使用`src/api`中的方法,不要自己写

## 代码简洁,减少<div>嵌套
